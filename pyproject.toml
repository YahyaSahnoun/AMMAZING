[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen-BestTeam"
version = "1.0.0"
description = "A reusable maze generation and solving module"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "typing-extensions",
]